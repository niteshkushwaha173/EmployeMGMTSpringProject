spring.application.name=employee-backend

# Loads application-local.properties (git-ignored) automatically for local runs.
# On Docker/Render just set the DB_* environment variables instead.
spring.profiles.default=local

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:employee_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Nitesh2026}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update

server.port=${SERVER_PORT:8284}
